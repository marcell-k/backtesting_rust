[package]
name = "backtesting"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = { version = "0.4.45", features = ["std"] }
rand = "0.10.2"
rayon = "1.12.0"

[lints.clippy]
too-many-arguments = "allow"

[profile.profiling]
inherits = "release"
debug = true
